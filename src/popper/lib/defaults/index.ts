import { defaultConfig } from '../../types'

const defaults: defaultConfig = {
  placement: 'bottom',
  trigger: 'hover',
  offset: 10,
  animate: true,
  speed: 400,
  zIndex: 2000
}

export default defaults;