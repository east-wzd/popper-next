export interface defaultConfig {
  placement?: 'top' | 'bottom' | 'left' | 'right',
  trigger?: 'hover' | 'click' | 'contextmenu'
  offset?: number,
  animate?: boolean,
  speed?: number,
  zIndex?: number
}